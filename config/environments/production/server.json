{
  "host": "${process.env.SERVER_HOST}",
  "port": "${process.env.SERVER_PORT}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}