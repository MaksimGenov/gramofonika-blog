{
  "host": "${process.env.STRAPI_SERVER_HOST}",
  "port": "${process.env.STRAPI_SERVER_PORT}",
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false,
    "path": "${process.env.STRAPI_CUSTOM_ADMIN_URI || '/admin'}"
  }
}
